apiVersion: batch/v1
kind: Job
metadata:
  name:  my-opslevel-report-deploy
  generateName: app-opslevel-report-deploy
  annotations:
    argocd.argoproj.io/hook: PostSync
    argocd.argoproj.io/hook-delete-policy: BeforeHookCreation
spec:
  template:
    spec:
      containers:
      - name: opslevel-report-deploy
        image: public.ecr.aws/opslevel/cli:latest
        command:
        - "opslevel"
        - "create"
        - "deploy"
        - "--integration-url"
        - "${OPSLEVEL_INTEGRATION_URL}"
        - "-s"
        - "jbtest"
        - "--environment"
        - "Prod"
        env:
          - name: OPSLEVEL_INTEGRATION_URL
            valueFrom:
              secretKeyRef:
                name: ops-level-generic
                key: url
      restartPolicy: Never
  backoffLimit: 1